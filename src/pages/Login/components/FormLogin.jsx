import React from 'react';
import styles from '../styles.module.scss';

const FormLogin = () => {
    const {a} = styles;
    return (
        <div>
            
        </div>
    );
};

export default FormLogin;